// 3. Crie uma função que calcula 5% de desconto retornando o valor do desconto.



let desconto = 0.05;
let valor = prompt("Quanto deu o valor da sua conta?");

function calcularDesconto(desconto, valor){
let resultado = desconto*valor;
return resultado;
}
