// Crie uma função que calcula 5% de desconto retornando o valor do desconto.

function desconto(b) {
    return (b * 5) / 100;
}

const valor = Number;
const resultado = desconto(valor);

console.log("O desconto de  5%  é de: " + resultado);

