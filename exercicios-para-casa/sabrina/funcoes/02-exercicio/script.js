// 2. Crie uma função que recebe 2 parâmetros e retorna a multiplicação deles.

function multiplica() {
    a = Number(prompt('um numero'))
    b = Number(prompt('outro'))

    return a * b;
}

alert (multiplica())